---
import Nav from "@components/Nav.astro";
import Footer from "@components/Footer.astro";

export interface Props {
  title: string;
  description?: string;
  noindex?: boolean;
  skipOG?: boolean;
  ogImage?: string;
}

const { title, description, noindex = false, skipOG = false, ogImage } = Astro.props;
const fullTitle = title.includes("Chinese Mountains") ? title : title + " | Chinese Mountains";
const siteUrl = "https://chinese-mountains.com";
const pageUrl = new URL(Astro.url.pathname, siteUrl).href;
const ogImageUrl = ogImage ?? siteUrl + "/images/og-default.png";
---

<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{fullTitle}</title>
  {description && <meta name="description" content={description} />}
  {noindex && <meta name="robots" content="noindex, follow" />}
  {!skipOG && <>
  <meta property="og:title" content={fullTitle} />
  {description && <meta property="og:description" content={description} />}
  <meta property="og:image" content={ogImageUrl} />
  <meta property="og:url" content={pageUrl} />
  <meta property="og:type" content="website" />
  <meta name="twitter:card" content="summary_large_image" />
  <meta name="twitter:title" content={fullTitle} />
  {description && <meta name="twitter:description" content={description} />}
  <meta name="twitter:image" content={ogImageUrl} />
  </>}
    <link rel="preconnect" href="https://a.basemaps.cartocdn.com" />
  <link rel="preconnect" href="https://b.basemaps.cartocdn.com" />
  <link rel="preconnect" href="https://c.basemaps.cartocdn.com" />
  
  <link href="/fonts/geist.css" rel="stylesheet" />
  <link rel="stylesheet" href="/leaflet/leaflet.css" />
  {!noindex && <link rel="canonical" href={new URL(Astro.url.pathname, siteUrl).href} />}
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
  <slot name="head" />
</head>
<body class="min-h-[100dvh] flex flex-col">
  <Nav />
  <main class="flex-1 pt-16">
    <slot />
  </main>
  <Footer />
</body>
</html>

<style is:global>
  @import "../styles/global.css";
</style>
